<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        @include('{{ name }}::components.head')
        @yield('style')
    </head>
    <body>
        <div id="root">
            <main class="">
                @yield('content')
            </main>
        </div>
        <script>
            window.app = {!! json_encode([
                // 'user' => $user,
                // 'dictionaryVersion' => $dictionaryVersion,
            ]) !!};
        </script>
        @yield('script')
    </body>
</html>
